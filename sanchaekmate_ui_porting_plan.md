# 산책메이트 UI 포팅 기획서

현재 `dog-walk_front`의 `features` 기반 구조, 라우팅, 의존성, 데이터 흐름은 유지하고, `sub_dog/dog_claude`의 UI 디자인만 현재 코드베이스 위로 이식한다. 이번 작업은 프로젝트 갈아타기나 구조 재설계가 아니라, 기존 제품 뼈대를 유지한 상태에서 화면 표현 계층만 2안 스타일로 재정비하는 UI 포팅 작업이다.

## 1. 작업 목표

- 기준 구조는 현재 프로젝트다.
- 기준 UI는 `sub_dog/dog_claude`다.
- 결과물은 "현재 프로젝트 구조 + 2안 UI"다.
- 이번 작업의 목적은 기능 확장이 아니라 시각 통합과 UX 정돈이다.

## 2. 핵심 원칙

### 유지할 것

- 현재 프로젝트의 `features/*` 기반 아키텍처와 feature 단위 책임
- 현재 route group `(app)`, `(auth)` 구조와 레이아웃 분리 방식
- 현재 `package.json` 기준 의존성 패키지
- 현재 mock 데이터, 유틸, 로컬 state 중심 흐름
- 현재 mock 데이터 기반 개발 방식

### 가져올 것

- 2안의 컬러 토큰, spacing, radius, shadow 체계
- 2안의 헤더, 하단 네비, 카드, 패널, 인증 화면 UI 톤
- 2안의 지도/목록/상세 패널 정보 배치 방식
- 2안의 정돈된 브랜드 표현과 화면 hierarchy

### 가져오지 않을 것

- `dog_claude`의 폴더 구조 전체
- `dog_claude`의 라우터 구조
- `dog_claude`의 `store/useStore.ts`
- `dog_claude`의 상태 아키텍처 (store 등)
- 신규 패키지 추가
- 2안 코드의 단순 복사/붙여넣기 방식 이식

## 3. 구현 기준

### 구조 유지 원칙

- 현재 프로젝트를 기준 저장소로 사용한다.
- 기존 route group `(app)`, `(auth)`는 유지한다.
- 컴포넌트 파일명과 export 경계는 가능한 한 유지한다.
- 외부 호출부 수정 범위는 최소화한다.
- 구조적 리팩터링보다 UI 포팅을 우선한다.

### 의존성 유지 원칙

- 현재 설치된 패키지만 사용한다.
- `framer-motion`, `zustand`, `leaflet` 등 2안 전용 패키지는 도입하지 않는다.
- 2안의 동작 방식은 현재 의존성 범위 안에서 재해석한다.

### 상태 및 데이터 원칙

- 현재 mock 데이터 shape를 유지한다.
- 2안에서 store 기반으로 처리하던 상태는 현재 구조에 맞게 `props`, `useState`, 기존 데이터 흐름으로 옮긴다.
- 새 전역 store는 만들지 않는다.

### UI 포팅 원칙

- 2안은 "디자인 레퍼런스"로 사용한다.
- 현재 코드베이스에 맞게 className, 레이아웃, 시각 hierarchy를 재작성한다.
- 복사보다 재구현을 우선한다.
- 1안의 강한 글래스 셸과 쇼케이스성 표현은 제거한다.

## 4. 화면별 적용 방향

### 공통 디자인 시스템

- 전역 토큰을 2안 스타일로 교체한다.
- 배경, 텍스트, 보더, 그림자, radius, 상태색을 2안 기준으로 정리한다.
- 기존 오렌지 계열 브랜드 컬러는 유지하되, 표현은 더 절제된 제품형 UI로 정리한다.

### 앱 셸

- 현재 전체 화면 글래스 컨테이너 구조를 제거한다.
- 데스크톱은 단정한 상단 헤더 구조로 정리한다.
- 모바일은 심플한 하단 탭 구조로 정리한다.
- 브랜드 표기는 전부 `산책메이트`로 통일한다.
- `InteractiveBackground`는 메인 앱에서 제거하고 인증 화면에서만 제한적으로 사용한다.

### 로그인 / 회원가입

- 현재 페이지 구조는 유지한다.
- 입력 필드, 버튼, 보조 링크, 로고 블록, 안내 카피를 2안 UI 톤으로 재정리한다.
- 인증 화면은 현재보다 가볍고 정돈된 인상을 목표로 한다.
- 폼 동작과 validation 흐름은 현재 프로젝트 기준을 유지한다.

### 지도 화면

- 현재 지도 페이지의 컨테이너 구조와 데이터 흐름은 유지한다.
- 검색, 필터 칩, 목록 카드, 지도 전환 버튼, 상세 패널/바텀시트를 2안 방식으로 재설계한다.
- 모바일은 `지도/목록` 전환이 분명해야 한다.
- 선택된 메이트는 모바일에서 하단 시트 형태로 보이게 한다.
- 데스크톱은 좌측 리스트, 중앙 지도, 우측 상세 읽기 흐름이 자연스럽게 보이도록 정리한다.
- 현재 더미 요청 액션은 기능 확장 없이도 시각 피드백이 더 명확하게 보이도록 만든다.

### 채팅 / 피드 / 마이페이지

- 2차 포팅 대상으로 둔다.
- 채팅은 일반 메신저보다 "산책 약속 조율" 맥락이 읽히게 정리한다.
- 피드는 생활형 카드 스타일과 지역 커뮤니티 톤을 강화한다.
- 마이페이지는 프로필 꾸미기보다 신뢰 정보 중심 구조로 정리한다.

## 5. 작업 순서

1. 전역 토큰 및 공통 스타일 정리
2. 앱 셸 UI 교체
3. 로그인 / 회원가입 UI 포팅
4. 지도 화면 UI 포팅
5. 채팅 / 피드 / 마이페이지 순차 포팅

## 6. 인터페이스 및 타입 기준

- 새로운 전역 store를 추가하지 않는다.
- 기존 route group `(app)`, `(auth)`와 현재 페이지 진입 구조를 유지한다.
- 기존 feature export 이름은 유지한다.
- 공용 UI 컴포넌트는 필요한 범위 내에서 스타일 확장만 허용한다.
- 2안 전용 데이터 shape는 직접 도입하지 않는다.

## 7. 완료 기준

### 레이아웃

- 데스크톱 헤더와 모바일 하단 탭이 정상적으로 표시된다.
- 인증 화면에서는 앱 네비가 숨겨진다.
- 메인 앱에서 전체 글래스 셸이 제거된다.

### 브랜딩

- 모든 핵심 화면에서 `VillageMate` 표기가 제거된다.
- `산책메이트` 명칭으로 통일된다.

### 인증 화면

- 로그인/회원가입 UI가 2안 톤으로 정리된다.
- 현재 이동 흐름과 입력 동작이 깨지지 않는다.

### 지도 화면

- 목록/지도 전환이 정상 동작한다.
- 카드 선택과 상세 시트/패널 노출이 정상 동작한다.
- 기존 mock 데이터 렌더링이 유지된다.

### 반응형

- 모바일과 데스크톱에서 레이아웃 붕괴 없이 읽힌다.
- 주요 스크롤 영역이 정상 동작한다.

## 8. 기본 가정

- "2안 UI만 가져온다"는 의미는 시각 디자인, 토큰, 컴포넌트 외형, 정보 배치 방식만 참조한다는 뜻으로 확정한다.
- 이번 작업은 구조 개편보다 UI 포팅을 우선한다.
- route group `(app)`, `(auth)`는 UI 포팅 대상이 아니며 유지한다.
- 기준 프로젝트는 현재 레포이며, 2안은 레퍼런스 소스다.
- 구현자는 현재 구조를 보존하는 방향으로만 수정한다.
